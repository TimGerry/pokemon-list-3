import { OrDirective } from './or.directive';

describe('OrDirective', () => {
  it('should create an instance', () => {
    const directive = new OrDirective();
    expect(directive).toBeTruthy();
  });
});
